[build]
  # 빌드 명령어
  command = "npm run build"
  # 빌드 결과물 디렉토리
  publish = "dist"

[build.environment]
  # Node.js 버전 지정
  NODE_VERSION = "20"

# SPA 라우팅 지원 (모든 경로를 index.html로 리다이렉트)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
